[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pictowebp"
version = "1.0.0"
description = "Blazingly fast bulk image-to-WebP converter with a CLI and a web UI."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"
authors = [{ name = "Aditya XQ" }]
keywords = ["webp", "image", "converter", "batch", "optimization"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Environment :: Web Environment",
    "Intended Audience :: End Users/Desktop",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Multimedia :: Graphics :: Graphics Conversion",
]
dependencies = [
    "pillow>=12.3",
    "tqdm>=4.70",
]

[project.optional-dependencies]
# The local web UI (FastAPI server). CLI-only installs skip these.
web = [
    "fastapi>=0.141",
    "uvicorn>=0.52",
    "python-multipart>=0.0.20",
]

[project.scripts]
pictowebp = "pictowebp.cli:main"
pictowebp-web = "pictowebp.web.app:main"

[dependency-groups]
dev = [
    "pytest>=9.1",
    "httpx2>=2.12",
    "ruff>=0.16",
    "pyright>=1.1.400",
    "fastapi>=0.141",
    "uvicorn>=0.52",
    "python-multipart>=0.0.20",
]

[tool.hatch.build.targets.wheel]
packages = ["src/pictowebp"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.pyright]
pythonVersion = "3.10"
include = ["src", "tests"]
typeCheckingMode = "standard"

[tool.ruff]
line-length = 100
target-version = "py310"
src = ["src", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "UP",  # pyupgrade
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "SIM", # flake8-simplify
    "RUF", # ruff-specific rules
]
